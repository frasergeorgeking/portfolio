---
// Base Bento Card Component
// Provides the shared card structure and styling for all bento card variants

interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<div
    class:list={[
        "group relative col-span-3 flex flex-col justify-between overflow-hidden rounded-2xl",
        "bg-background transform-gpu [box-shadow:0_-20px_80px_-20px_#ffffff1f_inset] [border:1px_solid_rgba(255,255,255,.1)]",
        className,
    ]}
>
    <!-- Background slot for custom backgrounds -->
    <div>
        <slot name="background" />
    </div>

    <!-- Main content slot -->
    <slot />

    <!-- Hover overlay effect -->
    <div
        class="pointer-events-none absolute inset-0 transform-gpu transition-all duration-300 group-hover:bg-black/[.03] group-hover:bg-neutral-800/10"
    >
    </div>
</div>
